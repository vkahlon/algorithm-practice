function solution(n) {
  const array = String(n).split('');
  const num = [];
  array.forEach(e => {
    num.push(Number.parseInt(escape));

  });
  return !!num.every(num => num % 2 === 0);
}
solution();
